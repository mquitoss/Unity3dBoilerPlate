using UnityEngine;
using System.Collections;

public class ApplicationGameControllerTester : MonoBehaviour
{
	public KeyCode onDieKey;
	public KeyCode onNextLevelKey;
	public KeyCode onPauseKey;
	public KeyCode onResumeKey;
	public KeyCode onBackToMenuKey;
	
	/*
	void Update ()
	{
		if ( Input.GetKeyDown( onDieKey ) ) {
			ApplicationGameController.Instance.onDie();
		}
		
		else if ( Input.GetKeyDown( onNextLevelKey ) ) {
			ApplicationGameController.Instance.onNextLevel();
		}

		else if ( Input.GetKeyDown( onPauseKey ) ) {
			ApplicationGameController.Instance.onPause();
		}

		else if ( Input.GetKeyDown( onResumeKey ) ) {
			ApplicationGameController.Instance.onResume();
		}

		else if ( Input.GetKeyDown( onBackToMenuKey ) ) {
			ApplicationGameController.Instance.onBackToMenu();
		}
	}
	*/
}
